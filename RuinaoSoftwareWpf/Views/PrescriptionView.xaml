<UserControl x:Class="RuinaoSoftwareWpf.Views.PrescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:RuinaoSoftwareWpf"
             Background="#11141D" Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="36"/><Setter Property="MinWidth" Value="104"/><Setter Property="Padding" Value="20,0"/>
            <Setter Property="Background" Value="{StaticResource Gold}"/><Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="Cursor" Value="Hand"/><Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="#252B36"/><Setter Property="Foreground" Value="#E6EAF1"/>
            <Setter Property="BorderBrush" Value="#3B4250"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value><ControlTemplate TargetType="Button">
                    <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Gold}"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate></Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrescriptionMenuItem" TargetType="MenuItem">
            <Setter Property="Height" Value="34"/><Setter Property="MinWidth" Value="112"/>
            <Setter Property="Padding" Value="16,0"/><Setter Property="Foreground" Value="#EEF1F6"/>
            <Setter Property="Background" Value="Transparent"/><Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/><Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="MenuRow" Background="{TemplateBinding Background}" CornerRadius="2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuRow" Property="Background" Value="#3A414F"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrescriptionContextMenu" TargetType="ContextMenu">
            <Setter Property="Background" Value="#202632"/><Setter Property="BorderBrush" Value="#485164"/>
            <Setter Property="BorderThickness" Value="1"/><Setter Property="Padding" Value="4"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F0F3F8"/><Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/><Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/><Setter Property="Foreground" Value="#A5AFC0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="DetailRow" TargetType="Border">
            <Setter Property="BorderBrush" Value="#292F3B"/><Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="18,16">
        <Grid.ColumnDefinitions><ColumnDefinition Width="320"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Border Grid.Column="0" Style="{StaticResource Card}" Padding="14,12">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="42"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Grid>
                    <TextBlock Text="处方" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <Button Content="＋" Width="32" Height="30" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Command="{Binding AddCommand}" Style="{StaticResource IconButton}" FontSize="18" ToolTip="新增处方"/>
                </Grid>
                <ListBox Grid.Row="1" ItemsSource="{Binding Prescriptions}" SelectedItem="{Binding SelectedPrescription}"
                         Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,2"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/><Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#343A47"/></Trigger>
                                <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#4A505D"/></Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:PrescriptionDefinition}">
                            <Grid Height="38" Background="Transparent">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="42"/></Grid.ColumnDefinitions>
                                <StackPanel Margin="10,0,4,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}" Foreground="#F2F4F8" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding StimulationType}" Foreground="#8E98AA" FontSize="10"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="..." Width="32" Height="26" Style="{StaticResource IconButton}" BorderThickness="0"
                                        Foreground="#C9D0DC" FontWeight="Bold" ToolTip="处方操作" Click="MoreButton_Click">
                                    <Button.ContextMenu>
                                        <ContextMenu Style="{StaticResource PrescriptionContextMenu}">
                                            <MenuItem Header="编辑" Style="{StaticResource PrescriptionMenuItem}"
                                                      CommandParameter="{Binding}" Click="EditMenuItem_Click"/>
                                            <MenuItem Header="复制" Style="{StaticResource PrescriptionMenuItem}"
                                                      CommandParameter="{Binding}" Click="CopyMenuItem_Click"/>
                                            <MenuItem Header="删除" Tag="delete" Foreground="#FF9292"
                                                      Style="{StaticResource PrescriptionMenuItem}"
                                                      CommandParameter="{Binding}" Click="DeleteMenuItem_Click"/>
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
        <Border Grid.Column="2" Style="{StaticResource Card}" Padding="22,12">
            <Grid DataContext="{Binding SelectedPrescription}">
                <Grid.RowDefinitions><RowDefinition Height="44"/><RowDefinition Height="1"/><RowDefinition Height="*"/><RowDefinition Height="64"/></Grid.RowDefinitions>
                <TextBlock Text="处方内容" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Border Grid.Row="1" Background="{StaticResource Line}"/>
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <Grid Margin="4,12,24,0" VerticalAlignment="Top" MaxWidth="820" HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="170"/><ColumnDefinition Width="540"/></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="42"/><RowDefinition Height="42"/><RowDefinition Height="42"/><RowDefinition Height="42"/>
                            <RowDefinition Height="42"/><RowDefinition Height="42"/><RowDefinition Height="42"/><RowDefinition Height="54"/>
                            <RowDefinition Height="42"/><RowDefinition Height="54"/>
                        </Grid.RowDefinitions>
                        <Border Grid.ColumnSpan="2" Style="{StaticResource DetailRow}"/>
                        <Border Grid.Row="1" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}" Background="#1A1F29"/>
                        <Border Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}"/>
                        <Border Grid.Row="3" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}" Background="#1A1F29"/>
                        <Border Grid.Row="4" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}"/>
                        <Border Grid.Row="5" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}" Background="#1A1F29"/>
                        <Border Grid.Row="6" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}"/>
                        <Border Grid.Row="7" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}" Background="#1A1F29"/>
                        <Border Grid.Row="8" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}"/>
                        <Border Grid.Row="9" Grid.ColumnSpan="2" Style="{StaticResource DetailRow}" Background="#1A1F29"/>
                        <TextBlock Margin="12,0" Text="适应症" Style="{StaticResource LabelText}"/><TextBlock Grid.Column="1" Text="{Binding Indication}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="1" Margin="12,0" Text="刺激模式" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding StimulationType}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="2" Margin="12,0" Text="电流强度" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CurrentDisplay}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="3" Margin="12,0" Text="模式" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding DeliveryMode}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="4" Margin="12,0" Text="总时长" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding TotalDurationDisplay}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="5" Margin="12,0" Text="间隔时间" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding IntervalDisplay}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="6" Margin="12,0" Text="单次时长" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding SessionDurationDisplay}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="7" Margin="12,0" Text="疗程" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding Course}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="8" Margin="12,0" Text="渐升/渐降" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding RampDisplay}" Style="{StaticResource ValueText}"/>
                        <TextBlock Grid.Row="9" Margin="12,0" Text="证据等级" Style="{StaticResource LabelText}"/><TextBlock Grid.Row="9" Grid.Column="1" Text="{Binding EvidenceGrade}" Style="{StaticResource ValueText}"/>
                    </Grid>
                </ScrollViewer>
                <StackPanel Grid.Row="3" Orientation="Horizontal" DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center">
                    <Button Content="使用处方" Style="{StaticResource ActionButton}" Command="{Binding UseCommand}"/>
                    <Button Content="导出处方" Style="{StaticResource ActionButton}" Command="{Binding ExportCommand}" Margin="16,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
