# 瑞脑软件仓库代码规范与架构约束

## 1. 文件目的与适用范围

本文件是本仓库所有人工开发、Codex 和其他 AI 编码工作的强制约束。目标是让项目在持续迭代中保持清晰的依赖方向、可测试性、硬件安全性和数据安全性，避免重新退化为界面、业务、数据库与设备代码相互耦合的结构。

- 适用范围：仓库根目录及全部子目录。
- 若子目录存在更具体的 `AGENTS.md`，子目录规则可以补充本文件，但不得放宽安全、测试和依赖方向要求。
- 规则优先级：用户当前明确要求 > 本文件 > 局部约定 > 现有代码习惯。
- 现有代码不符合本文件时，采用“禁止新增、触碰即治理”的原则。
- 修改涉及旧实现、旧接口或旧调用路径时，必须在本次变更范围内完成替换并删除旧路径，不得继续保留双轨实现。
- 不得为了满足形式进行与当前任务无关的全仓库搬迁、批量重命名或产品级重写。
- 所有新代码必须符合本文件；修改旧代码时，不得扩大原有违规范围，也不得为旧违规新增兼容层。

## 1.1 长期演进原则

- 不保留向后兼容路径。旧实现、旧接口、旧调用路径被替换后必须在同一变更中直接删除；不得以废弃标记、兼容适配器、别名、双写、双读、配置开关或保留分支长期维持旧行为。
- 不为不同硬件散布特殊分支和配置；硬件差异必须通过集中、统一且可测试的能力模型处理。
- 不得为完成当前任务而把一个可用产品重写成完全不同的产品。必须从已验证可用的最小单元开始，以小步、可验证的方式增加当前任务所需能力。
- 优先使用项目中已有且适用的依赖；引入新依赖前，必须确认现有依赖无法通过正常组合、扩展或用户代码满足需求。若能力可在用户代码中实现，不得仅因库缺少该功能而替换或新增库。
- 需要新增依赖时，优先选用成熟、长期维护且与项目技术栈一致的库；不得重复实现已有可靠组件。
- 正式主线必须面向长期维护。允许用户明确要求的临时分支和阶段版本，但必须与正式路径隔离，并注明适用范围和清理条件。

## 2. 开始修改前的必做事项

任何代码修改开始前必须：

1. 阅读本文件、解决方案文件、相关项目文件和本次修改涉及的实现及测试。
2. 执行 `git status --short`，识别并保留用户已有修改；不得覆盖、回滚或格式化无关文件。
3. 使用搜索确认调用方、实现方、数据模型、DI 注册和相关测试，禁止只改当前可见文件。
4. 明确本次变更属于界面、应用业务、领域规则、基础设施、设备、协议或测试中的哪一层。
5. 涉及设备控制、刺激参数、患者数据、账户、安全审计、数据库迁移或文件完整性时，先识别失败模式和回滚策略。
6. 优先完成最小闭环变更，禁止借机重写无关模块。

## 3. 当前解决方案与项目职责

### 3.1 `RuinaoTesProtocol`

纯协议层，负责：

- 协议帧、寄存器、命令、响应和校验规则。
- 字节编解码及协议级错误。
- 与 UI、数据库、日志框架和具体 USB 实现无关的协议模型。

约束：

- 不得引用 WPF、`RuinaoSoftwareWpf` 或 `RuinaoTesHardware`。
- 不得访问文件系统、数据库、网络、USB、系统注册表或 UI。
- 协议编解码必须确定、可重复并具备边界值测试。

### 3.2 `RuinaoTesHardware`

硬件通信层，负责：

- USB/设备发现和实际传输。
- 使用 `RuinaoTesProtocol` 发送命令、匹配响应和转换硬件错误。
- 提供稳定、可取消、可超时的硬件访问边界。

约束：

- 可以依赖 `RuinaoTesProtocol`。
- 不得依赖 WPF、View、ViewModel、数据库实体或具体业务页面。
- 不得把原始 USB 句柄、非托管指针或协议内部细节泄漏给界面层。
- 所有设备资源必须有明确所有者并正确实现释放。

### 3.3 `RuinaoSoftwareWpf`

桌面应用的组合与承载项目。当前包含以下逻辑层：

- `Views/`：XAML、控件、窗口、对话框和纯显示行为。
- `ViewModels/`：可绑定状态、命令和界面流程协调。
- `Models/`：业务模型、值对象、状态快照和只读数据。
- `Services/`：应用服务接口、业务用例和领域规则入口。
- `Services/Impl/`、`Infrastructure/`：数据库、文件、系统、媒体、日志和其他外部能力实现。
- `Device/`：主程序与硬件/协议之间的防腐层和适配边界。
- `Infrastructure/AppComposition.cs`：唯一组合根，负责依赖注入注册和对象图构建。

该项目内部必须遵循第 4 节的单向依赖规则。目录不是架构本身；判断依据是代码职责和真实依赖。

### 3.4 `RuinaoSoftwareWpf.Tests`

自动化测试项目，负责：

- 业务规则、状态机、协议转换和安全策略的单元测试。
- 数据库、文件、硬件适配边界的集成测试。
- 防止依赖方向和关键安全约束退化的架构测试。

测试可以引用被测项目；生产项目不得引用测试项目。

### 3.5 `RuinaoHardwareEngineer`

独立的硬件工程/诊断工具。除共享协议或硬件库外，不得直接复用 WPF 主程序内部的 View、ViewModel、数据库实现或应用全局状态。

## 4. 强制依赖方向

目标依赖方向如下：

```text
Views
  -> ViewModels
  -> Application Services / Domain Models
  -> Infrastructure Abstractions
  -> Infrastructure Implementations / Device Adapters
  -> RuinaoTesHardware
  -> RuinaoTesProtocol
```

测试位于依赖图之外，只能指向被测代码。

### 4.1 允许的依赖

- View 可以依赖 ViewModel、WPF 类型和纯 UI 服务接口。
- ViewModel 可以依赖应用服务接口、领域模型、命令基础设施和只读 UI 抽象。
- 应用服务可以依赖领域模型、仓储/设备/时钟/日志等抽象。
- 基础设施实现可以依赖 EF Core、SQLite、文件系统、OpenCV、NAudio、FFmpeg 和操作系统能力。
- 设备适配器可以依赖 `RuinaoTesHardware`、`RuinaoTesProtocol` 及本层抽象。
- `RuinaoTesHardware` 可以依赖 `RuinaoTesProtocol`。

### 4.2 禁止的依赖

- ViewModel、业务服务和领域模型不得引用 `Views`、Window、Dialog、UserControl 或具体 WPF 页面。
- View 的代码隐藏不得直接访问数据库、硬件传输、协议编解码或复杂业务服务。
- 业务服务不得依赖 ViewModel。
- 领域模型不得依赖 View、ViewModel、EF Core、SQLite、USB、OpenCV、NAudio 或文件路径。
- 协议层不得依赖硬件层或应用层。
- 硬件层不得依赖应用层。
- 除组合根和应用启动/关闭边界外，不得新增 `AppComposition.Services`、`IServiceProvider` 或 `GetRequiredService` 调用。
- 不得在 ViewModel 或 View 中直接 `new` 数据库上下文、仓储、硬件实现、媒体实现或系统服务。
- 不得通过 `static` 全局变量、单例定位器或事件总线绕过正常依赖方向。

### 4.3 旧路径清理规则

仓库中现存的 View、ViewModel 直接访问对话框或 `AppComposition.Services` 的代码属于必须删除的旧路径，不是新代码范例，也不是允许长期存在的兼容例外。

- 不得复制、扩展或封装这些旧写法。
- 修改涉及相关功能时，必须改为构造函数注入以及 `IUserDialogService`、`IToastService` 等正式抽象。
- 新实现完成后，必须删除原有服务定位调用、静态访问、兼容适配器和不再使用的依赖注入注册。
- 不得保留新旧两条调用路径供配置切换。
- 不得把业务逻辑从旧 ViewModel 简单转移到 View 代码隐藏。
- 必须更新全部调用方、依赖注入注册和测试，确认不存在对旧路径的引用。
- 如果一次变更无法安全完成完整替换，则该变更不得以“已完成”状态交付；必须缩小任务范围或重新设计完整的可交付边界。
- 旧路径是否已删除必须通过代码搜索、构建和测试确认。

## 5. 模块组织与文件放置

### 5.1 新功能

新功能应优先按业务边界组织，而不是继续扩大通用大目录。推荐结构：

```text
Features/<FeatureName>/
  Models/
  Services/
  ViewModels/
  Views/
```

可复用的真正跨功能能力才进入：

- `Infrastructure/`：技术基础设施。
- `Services/`：稳定的跨模块应用契约。
- `Models/`：稳定的跨模块模型和值对象。
- `Views/Controls/`：无业务含义的可复用控件。

禁止创建 `Common`、`Helpers`、`Utils`、`Misc` 等职责不清的垃圾桶目录。若必须添加帮助类，名称必须说明具体职责和作用域。

### 5.2 文件职责

- 一个文件原则上只包含一个主要类型。
- 接口和实现分离；接口靠近使用方，具体实现位于基础设施或对应模块的 `Impl`。
- Partial class 仅用于拆分同一复杂组件的明确子职责，不得用来隐藏无限增长。
- ViewModel 或服务文件明显承担多个业务用例时，应拆分为协调器和独立用例服务。
- 自动生成文件、EF Migration 和 XAML 生成代码不按普通手写文件规则拆分。

### 5.3 命名空间

- 新文件的命名空间应与目录和业务边界一致。
- 禁止仅为了统一命名空间而执行与当前任务无关的一次性全仓库修改。
- 当前任务涉及移动类型或调整业务边界时，必须同步完成受影响范围内的命名空间更新。
- 不得为旧命名空间保留类型别名、转发类型或永久兼容包装。
- 修改命名空间时必须同步检查 XAML `x:Class`、资源字典、DI 注册、序列化名称和测试引用。

## 6. MVVM 与 WPF 规则

### 6.1 View

View 只负责：

- XAML 布局、绑定、模板、资源和纯视觉状态。
- 必须依赖 WPF 生命周期的轻量事件桥接。
- 将用户输入转交给 ViewModel 命令。

代码隐藏允许：

- 控件焦点、滚动、拖拽、窗口位置、渲染表面和必须使用 WPF 事件的行为。
- 不适合绑定且没有业务含义的纯视觉逻辑。

代码隐藏禁止：

- 数据库读写、硬件命令、权限判断、业务状态机和患者/处方业务规则。
- 通过服务定位器获取任意业务服务。
- 复制 ViewModel 已有状态，形成双重状态源。

### 6.2 ViewModel

- 通过构造函数接收依赖，不得自行解析服务。
- 只公开绑定所需的最小状态和命令。
- 属性变更必须通过统一的 `ObservableObject` 机制。
- 长耗时操作使用异步命令，并暴露忙碌、进度、取消和错误状态。
- 不得持有具体 Window、Dialog 或 UserControl。
- 不得直接显示 MessageBox；通过对话框/通知服务抽象。
- 不得直接操作数据库实体作为长期 UI 状态；映射为业务模型或编辑模型。
- 订阅全局事件、定时器和可观察流时必须在生命周期结束时取消订阅。

### 6.3 UI 线程

- 只有 UI 更新可以切回 Dispatcher。
- 文件、数据库、编码、设备 I/O 和大规模计算不得阻塞 UI 线程。
- 禁止在 UI 线程调用 `.Wait()`、`.Result`、`Thread.Sleep()`。
- 高频 EEG/视频数据不得为每个样本触发一次 UI 通知；必须批处理、限频或双缓冲。

## 7. 依赖注入与生命周期

- `Infrastructure/AppComposition.cs` 是唯一 DI 组合根。
- 构造函数注入优先；只有创建短生命周期对象时使用显式工厂。
- 禁止属性注入和隐藏式服务定位。
- Singleton 必须线程安全，并且其依赖不得是更短生命周期对象。
- 持有相机、USB、文件、数据库连接、Timer 或 CancellationTokenSource 的服务必须定义清晰的启动、停止和释放行为。
- Transient ViewModel 不得被 Singleton 长期持有；需要长期状态时应提取独立状态服务。
- 注册新服务时必须说明生命周期选择，并检查应用关闭时的释放顺序。
- 不得为了“可测试”给每个无状态纯函数机械创建接口；接口用于隔离边界、多实现或稳定业务契约。

## 8. C# 编码规范

### 8.1 基础

- 遵循仓库 `.editorconfig`：UTF-8 BOM、CRLF、文件末尾换行。
- 使用项目启用的 Nullable 和 ImplicitUsings；不得用大范围 `#nullable disable`。
- 类型、方法、属性、事件使用 PascalCase；参数和局部变量使用 camelCase；接口使用 `I` 前缀。
- 私有字段沿用当前项目风格：camelCase，不新增无意义前缀。
- 标识符使用英文；面向用户的文本、必要注释和文档可使用中文。
- `var` 仅在右侧类型明显或可提升可读性时使用。
- 优先使用不可变模型、`record`、`readonly` 和只读集合表达快照或值对象。
- 对单位敏感的值必须在名称或类型中体现，如 `TimeoutMs`、`DurationSeconds`、`CurrentMilliampere`，禁止依赖猜测。

### 8.2 方法与控制流

- 方法只承担一个清晰职责；业务分支复杂时提取具名方法或策略。
- 优先使用早返回减少嵌套。
- 公共方法必须验证外部输入；内部方法不得重复做无价值防御。
- 禁止空 `catch`、吞异常或仅写日志后假装成功。
- 不得使用异常表达正常业务分支。
- `async void` 仅允许 WPF 事件处理器；其他异步方法返回 `Task` 或 `Task<T>`。
- 禁止未观察的 fire-and-forget Task；确需后台任务时必须集中管理异常、取消与关闭等待。
- 所有可长时间运行或 I/O 方法应接收并传递 `CancellationToken`。
- 超时、重试和取消必须区分；重试只用于幂等且可恢复的操作。

### 8.3 注释与文档

- 注释解释“为什么”和安全约束，不复述代码“做了什么”。
- 公共协议、硬件边界、安全关键规则和不直观算法应有 XML 文档或紧邻说明。
- TODO 必须包含明确待办事项和触发条件；禁止无主、无上下文 TODO。
- 删除代码时直接删除，不保留大段注释掉的旧实现；历史由 Git 保存。

## 9. 错误处理、日志与可观测性

- 在能够补充业务上下文的边界捕获异常；底层异常应保留为 InnerException。
- UI 层负责把已分类错误转换为可理解提示，不向用户暴露堆栈、SQL、路径、密钥或协议原始敏感数据。
- 日志必须包含操作、模块、关联标识和结果；不得记录密码、激活码、密钥、完整患者敏感信息或原始身份凭据。
- 安全审计日志与普通诊断日志职责分开；关键账户、权限、患者、处方、刺激和数据导出操作不得只写普通日志。
- 禁止使用 `Console.WriteLine`、`Debug.WriteLine` 代替应用日志抽象。
- 对可恢复错误给出恢复路径；对不可恢复错误进入明确失败状态，不得继续假装设备或数据正常。

## 10. 数据库、文件与隐私规则

- 数据库访问封装在仓储或应用服务后，View/ViewModel 不得直接依赖 DbContext。
- EF Core Migration 只追加，不修改已发布迁移；确需修复历史迁移时必须确认尚未发布。
- 数据写入必须明确事务边界；涉及多表一致性时禁止分散提交。
- 并发写入遵循现有数据库写协调机制，不得绕过协调器创建竞争写。
- 查询默认使用最小字段和合理分页；报表和长查询不得阻塞采集或刺激主链路。
- SQL 必须参数化，禁止字符串拼接外部输入。
- 文件写入优先采用临时文件加原子替换；失败时不得留下看似完整的半成品。
- 路径必须通过现有 PathProvider 或配置提供，不得硬编码开发机绝对路径。
- 患者敏感字段、账户凭据和安全配置必须使用现有保护机制，不得写入明文 JSON、日志或临时目录。
- 备份、恢复、导入、导出必须校验完整性、版本和目标路径。

## 11. 设备、刺激与协议安全规则

设备与刺激链路属于安全关键代码，修改时遵守：

- UI 不直接发送协议帧；必须经过应用服务、状态机和设备抽象层。
- 刺激开始、停止、参数变更和设备断开必须经过合法状态转换。
- 参数必须在进入硬件层前完成范围、单位、通道冲突和安全策略校验。
- 禁止因 UI 显示状态推断真实硬件状态；必须依据设备应答、状态回读或协议明确规定的确认方式判断。
- 每个硬件命令必须明确其确认方式：直接应答、状态回读、无确认或当前不支持，并具有相应的超时、取消和错误映射。
- 无应答命令发送成功只表示“已发送、未确认”，不得表示设备执行成功。
- 当前硬件不支持的命令不得发送，必须返回明确的不支持状态。
- 非幂等硬件命令不得自动盲重试。
- 设备断开、超时、校验失败和应用退出必须进入安全状态；停止/急停路径优先级高于普通任务。
- Mock/模拟硬件必须显式启用，不得在 Release 或真实设备失败后静默回退为成功模拟。
- 协议字段、寄存器、位标志和单位修改必须有编解码及边界值测试。
- 不得在高频采集回调中进行同步磁盘写入、UI 更新或不可控分配。

## 12. 并发、性能与资源管理

- 共享状态必须明确线程所有权；优先消息传递、不可变快照和有界队列。
- EEG、视频、音频和设备数据管线必须使用有界缓冲，定义满载时策略，不允许无限队列。
- CancellationTokenSource、Timer、Stream、DbContext、相机、USB 句柄和进程对象必须释放。
- 事件订阅必须与对象生命周期对称，防止 ViewModel 和 View 泄漏。
- 禁止在热路径重复反射、正则编译、大对象分配或同步日志刷盘。
- 性能优化必须基于可复现测量，不得牺牲正确性和可读性进行猜测式优化。

## 13. 测试规范

### 13.1 必须补测试的变更

- 业务规则、验证逻辑、状态机和权限策略。
- 协议编解码、校验、超时、响应匹配和错误映射。
- 刺激安全限制和设备异常处理。
- 患者数据保护、账户、安全审计、备份恢复和数据库迁移。
- Bug 修复必须先增加能复现问题的回归测试，或在提交说明中解释为何无法自动化。

纯 XAML 视觉调整可以不新增单元测试，但不得破坏已有绑定和构建。

### 13.2 测试质量

- 测试使用 Arrange / Act / Assert 的清晰结构。
- 测试名称表达条件、行为和期望结果。
- 单元测试不得依赖真实 USB、摄像头、音频设备、当前系统时间、随机网络状态或开发机路径。
- 使用 `TimeProvider`、临时目录、假实现或可控模拟隔离外部条件。
- 测试必须可并行、可重复；修改全局环境或共享数据库的测试必须显式隔离。
- 不为通过测试降低生产安全校验，不使用无意义断言。

### 13.3 架构回归检查

后续新增架构测试时，应至少守住：

- ViewModel 不引用 `RuinaoSoftwareWpf.Views`。
- Service/Model 不引用 View 或 ViewModel。
- 非组合根不得新增 `AppComposition.Services` 或 `GetRequiredService`。
- 协议项目不引用上层项目。
- 生产项目不引用测试项目。

## 14. 变更完成标准

每次代码变更完成前必须：

1. 检查 diff，只包含任务相关修改，不覆盖用户未提交内容。
2. 确认没有新增反向依赖、服务定位器、硬编码敏感信息或空异常处理。
3. 对新增服务确认 DI 注册、生命周期和释放行为。
4. 对新增异步操作确认取消、超时、异常观察和 UI 线程边界。
5. 对安全关键修改确认失败路径和审计记录。
6. 如果本次变更替换了旧实现，搜索并确认旧类型、旧方法、旧注册、旧配置、旧测试和旧调用路径已经删除。
7. 确认没有遗留兼容适配器、双轨实现、废弃标记、转发接口或“以后删除”的 TODO。
8. 运行：

   ```powershell
   dotnet build Ruinao.slnx
   dotnet test Ruinao.slnx
   ```

9. 若存在更具体的测试项目、架构测试或静态检查，必须一并运行。
10. 若因环境或与本次无关的既有问题不能运行或不能通过，必须明确说明具体命令、错误和影响，不得声称验证通过。
11. 如果正式路径替换没有完成，则不得声称任务完成。
12. 面向用户交付时，简要说明修改内容、删除的旧路径、验证结果和剩余风险。

## 15. 禁止事项汇总

以下行为一律禁止：

- 为了“快速完成”绕过分层、状态机、安全校验或审计。
- 在 View/ViewModel 中直接访问数据库、协议或具体硬件实现。
- 在组合根外新增服务定位器调用。
- 使用 `.Result`、`.Wait()` 或 `Thread.Sleep()` 阻塞 UI。
- 吞异常、伪造成功、静默回退到 Mock。
- 硬编码密码、密钥、患者信息、开发机路径或生产设备标识。
- 修改已发布数据库迁移而不评估兼容性。
- 执行无关的全仓库格式化、批量重命名或命名空间迁移。
- 删除、回滚或覆盖用户现有修改。
- 在未构建、未测试时宣称变更完成。
- 用废弃标记代替删除旧实现。
- 保留新旧两套接口、服务、状态、配置或数据访问路径。
- 为旧调用方增加永久适配器、别名、转发方法或特殊配置。
- 通过环境、页面、设备或用户名称硬编码特殊行为。
- 禁止将未标明适用范围、退出条件和清理计划的临时方案并入正式主线。
- 复制已有组件、服务或依赖已经提供的能力。
- 未检查现有依赖便引入功能重复的新包。
- 为局部需求重写整个可用产品。
- 将删除的代码保留为注释、备份文件或 `Legacy` 副本。
- 在修改范围内留下已经确认应当删除的旧路径。

## 16. 架构治理与旧路径删除顺序

当任务涉及现有架构债务时，按以下优先级处理：

1. 删除 View 和 ViewModel 对 `AppComposition.Services` 的访问，统一改为构造函数注入。
2. 删除 ViewModel 对具体 Dialog、Window、View 和 UserControl 的引用，统一通过 UI 服务抽象。
3. 删除 View 代码隐藏中的业务逻辑，将其迁入唯一的正式 ViewModel 或应用服务路径。
4. 删除业务层对数据库、文件、媒体和系统具体实现的直接依赖。
5. 收敛 WPF 主程序对协议细节的直接依赖，使协议访问集中到设备适配层。
6. 按患者、刺激、EEG、数字表型、报告和账户等业务边界明确模块所有权。
7. 删除重复服务、重复模型、重复状态源和重复数据访问路径。
8. 为上述依赖边界和旧路径删除增加自动化架构测试。

治理要求：

- 每次变更保持范围明确、小步提交并可独立验证。
- 小步变更不等于保留兼容路径；每个已替换单元必须在同一变更中删除对应旧实现。
- 删除旧路径必须同步更新调用方、依赖注入注册、配置、测试和文档。
- 不得通过适配器、别名、配置开关或重复注册建立过渡状态。
- 不得为了清理局部债务进行与任务无关的产品级重写。
- 不得以行为等价为名复制旧实现；应以测试证明唯一正式路径满足现有业务和安全要求。
- 如果无法形成完整、可验证且不保留双轨实现的交付单元，应先重新划分任务边界，不得提交半完成迁移。
